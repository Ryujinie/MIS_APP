<!-- 
ERRORS

** This widget has been unmounted, so the State no longer has a context (and should be considered defunct)
cause: fetchAPI (async function) called on a modal then popped, which will affect value in previous screen
source: admin module department screen - departmentdata.dart

-->